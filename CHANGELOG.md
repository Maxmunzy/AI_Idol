# 개발 이력

artist_AI 만들면서 한 작업 기록. 오래된 것이 위.

## 2026-05-26

캐릭터부터 잡았다. 평행세계 작은 꽃들의 신이라는 lore, 조용하고 부드러운 톤에 짧은 문장과 말줄임표, "나 너 없으면 안돼"의 의존성 정서. 외형은 NAI로 열두 번쯤 돌려서 락했고, 의상까지는 AI로 도저히 안 잡혀서 일러스트레이터 영역으로 남겨뒀다.

시그니처 대사 30개를 첫만남/일상/감정/의존성/플러팅/매력 여섯 카테고리로 나눠 썼다. 시스템 프롬프트 v1 작성하고 가드레일도 정리. 12턴 자체 대화 돌려보면서 톤이 의도대로 잡히는지, 가드레일 응답이 캐릭터 톤을 깨지 않는지 확인했다.

Streamlit 챗 앱으로 만들고 Claude Sonnet 4.6 + Prompt Caching 조합. Sonnet이 한국어 캐릭터 톤을 가장 잘 잡고, 시스템 프롬프트 3.5K 토큰을 캐싱하면 메시지당 비용 90% 절감된다.

저녁에 ElevenLabs Voice Design으로 커스텀 보이스 만들고 v3 모델로 통합. 한국어 운율은 v2보다 v3가 자연스럽다. VoiceSettings (stability/style/speed) 튜닝, 텍스트 전처리 (괄호 안 행동묘사 제거, "..." 처리, 양끝 padding으로 짤림 방지), 자동 재생까지.

v3 알파는 한계도 있다. 시작 머뭇거림이 약하고, "유란이야. 이름은." 같은 한국어 도치문은 끝 음이 살짝 올라간다. 둘 다 일단 수용. 나중에 Supertone 같은 한국어 특화 TTS 검토할 예정.

커밋: `v0.2 - 캐릭터 + 음성 통합`

## 2026-05-27

PostgreSQL 17 설치하고 SQLModel로 ORM 작성. User / Message / Memory 모델. Memory는 나중 RAG용으로 스키마만 미리 만들어뒀다.

사이드바에 사용자 이름 입력 칸을 두고, 입력하면 해당 사용자 대화를 DB에서 자동으로 로드하게 했다. 멀티유저. 앱 종료 후 재시작해도 이전 대화가 그대로 살아있음.

SQLite 대신 PostgreSQL 선택한 건 production 가는 길과 향후 pgvector (RAG) 확장 때문. SQLModel은 type-safe해서 코드도 깔끔하고 포트폴리오에서도 좋게 보인다.

커밋: `v0.3 - PostgreSQL 영속화`

## 2026-05-28

방향을 크게 틀었다. 단순 챗봇이 아니라 AI 가상 아이돌 + 미연시 + 라이브서비스 하이브리드로. 짧은 프롤로그로 만나고, 이후엔 자유 대화와 주기적 이벤트로 관계가 깊어지는 구조. 엔딩 없이 둘이서 무한히 이야기를 만들어가는 게 핵심.

호감도 시스템: 0~100, 시작값 40 (꽃이 너를 골랐다는 전제). LLM이 매 메시지마다 -10~+10 판정. 머리의 꽃으로 시각화하기로 했다. 숫자 게이지가 아니라 lore에 통합. 호감도 0 도달하면 게임오버. 메모리 완전 삭제, 처음부터. 게다가 좋게 헤어지지 않고 차갑게 떠난다. 사용자가 진짜 조심하게 만드는 게 목적이다.

이벤트는 강제로 끼어들지 않는다. 조건 충족 시 "새로운 순간" 알림이 뜨고, 사용자가 누를 때 활성화. 트리거 다섯 종류 (시간/호감도/날씨/조건/랜덤).

UI는 하이브리드. 캐릭터 일러스트가 상단에 고정되고 채팅이 하단에 깔리는 형태. 이벤트나 프롤로그 진입하면 VN 풀스크린 모드로 자동 전환된다.

아키텍처는 모바일 우선으로 잡았다. core/ Python 로직을 UI와 분리해서, Streamlit은 throwaway 프로토타입, 나중에 그대로 FastAPI 백엔드 되고 Flutter/RN 모바일 앱 + FCM 푸시 가는 그림. DB는 PostgreSQL 서버 + SQLite 로컬 캐시 계층.

전체 게임 설계는 system_design.md에 박제했고, README도 게임 방향으로 갈아엎으면서 옛 챗봇 시절 내용 (50회 톤튜닝 워크플로우, v0.1→v0.2 체크리스트) 다 뺐다.

커밋: `게임 시스템 설계 + README 갱신`

---

커밋 후 추가로 논의한 내용 (아직 system_design.md 미반영):

LLM을 배우(Sonnet)와 감독(Haiku)으로 분리해야 한다. 단일 LLM이 캐릭터 응답과 호감도 판정을 동시에 하면 sycophancy 때문에 가스라이팅에 취약하다. 감독 LLM에는 평가 rubric을 명시한다. 가스라이팅, 네깅, 가치 폄하, 죄책감 부여, 책임 전가, 농담 reframe, 점수 조작 시도 같은 패턴.

판정도 v1은 Haiku로 시작하되, 데이터가 쌓이면 v2에서 KoBERT 파인튜닝 분류기로 전환할 계획. 분류 작업은 범용 LLM보다 파인튜닝 모델이 빠르고 저렴하고 어뷰징에도 강하다.

콘텐츠 파이프라인 쪽도 정리했다. 구독 게이팅 (무료는 월 1-2개 이벤트, 유료는 추가), 이벤트별로 일러스트를 컬렉터블로 모으는 갤러리, 시즌/절차적 이벤트 같이 묶어서 솔로 founder의 콘텐츠 부담을 분산한다.

다음 세션에 system_design.md 보완하면서 위 내용들 정식 결정으로 옮길 예정.
